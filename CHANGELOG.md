# Changelog

## 0.1.0

- Added CLI commands for `scan`, `clean`, `pr`, `init`, and `version`.
- Added analyzers for unused gems, redundant lockfile dependencies, and version-based cleanup hints.
- Added terminal, JSON, and YAML formatters.
- Added Gemfile editing with backup support and GitHub PR automation.
